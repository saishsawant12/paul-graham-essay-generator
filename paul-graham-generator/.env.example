# Example environment variables for local development
# Copy this to a file named .env and fill in your real values.

# OpenAI API key (keep secret — never commit to git)
OPENAI_API_KEY="sk-your-new-key"

# Optional: model name to use with the OpenAI client
#MODEL="gpt-4o-mini"
